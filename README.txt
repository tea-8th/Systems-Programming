I have same working project in both the folders.

The only difference is in library folder, 'function.c' file is made into a static library file and in multiple folder it is used as a normal .c file.

You can run both the programs from shell script file using './run.sh' command.